import { PageFactory } from "./PageFactory.js";
export class ProposalRequests extends PageFactory {
    constructor() { super({ title: "Proposal Requests", subtitle: "Request, compare, and route pricing for owner-directed scope changes.", icon: "send" }); }
}
