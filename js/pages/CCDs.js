import { PageFactory } from "./PageFactory.js";
export class CCDs extends PageFactory {
    constructor() { super({ title: "CCDs", subtitle: "Coordinate construction change directives from issuance through cost resolution.", icon: "files" }); }
}
